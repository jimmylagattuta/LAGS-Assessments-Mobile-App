//  index.js Placce code in here for android!!!
import { AppRegistry } from 'react-native';
import App from './src/app';

// Render it to the device
AppRegistry.registerComponent('LAGS_Assessments', () => App);
